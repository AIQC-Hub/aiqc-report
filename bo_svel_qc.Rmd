---
title: "Sound velocity QC"
output:
  html_document:
    theme: flatly
---

```{r include = FALSE}
knitr::opts_chunk$set(echo=FALSE)
```

```{r, echo = FALSE, message = FALSE, warning = FALSE}
r_func_path <- "./_func"
r_funcs <- c("libraries.Rmd", "common.Rmd", "netcdf.Rmd", "qc.Rmd")
```

```{r, import, child=file.path(r_func_path, r_funcs)}
```

```{r, echo = FALSE, message = FALSE, warning = FALSE}
# Path
rsc_dir <- "./resources"
#rsc_dir <- "/scratch/data/aiqc/htmlreport/nrt_bo/resources"

# Variables
var <- "svel"
qc_var <- "svel_qc"
qc_var_name <- "Sound velocity QC flag"

# Data frame
bo_file_name <- file.path(rsc_dir, "netcdf_nrt_bo.parquet")
df_bo <- read_parquet(bo_file_name)

```

The page shows the basic analysis of `r tolower(qc_var_name)` QC flags from `r source`.

---

```{r, basic_info, results = 'asis'}
res <- knitr::knit_expand(qc_basic_info, qc_var=qc_var, qc_var_name=qc_var_name, input=input,
                          input_name=input_name, input_url=input_url)
cat(res, sep = '\n')
```

```{r, summary_stats, results = 'asis'}
src <- knitr::knit_expand(qc_summary_stats, df=df_name, var=var, qc_var=qc_var)
res <- knitr::knit_child(text = src, quiet = TRUE)
cat(res, sep = '\n')
```
